# ===========================================
# Macroscope PR Creator - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values
# Required variables are marked with [REQUIRED]

# -------------------------------------------
# GitHub OAuth (for user authentication)
# -------------------------------------------
# [REQUIRED] Create OAuth App at: https://github.com/settings/developers
# Set callback URL to: http://localhost:3000/api/auth/callback/github (local)
# Or: https://your-domain.com/api/auth/callback/github (production)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# -------------------------------------------
# NextAuth Configuration
# -------------------------------------------
# [REQUIRED] Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=
# App URL (set automatically on Railway, required locally)
NEXTAUTH_URL=http://localhost:3000

# -------------------------------------------
# GitHub Bot Token
# -------------------------------------------
# [REQUIRED] Personal access token for creating PRs
# Required permissions: repo (full control of private repositories)
# Generate at: https://github.com/settings/tokens
GITHUB_BOT_TOKEN=

# -------------------------------------------
# Anthropic API (for PR Analysis)
# -------------------------------------------
# [REQUIRED] API key for Claude
# Generate at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# -------------------------------------------
# Custom Paths (optional)
# -------------------------------------------
# These have sensible defaults that work for both local and Railway
# Only override if you have specific requirements
# DATA_DIR=/data
# DB_PATH=/data/pr-creator.db
# REPOS_DIR=/data/repos

# -------------------------------------------
# Railway-specific (set automatically by Railway)
# -------------------------------------------
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PROJECT_ID=
# RAILWAY_SERVICE_ID=
