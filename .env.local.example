# GitHub Bot Token (for macroscope-gtm-bot)
# This token authenticates as the bot account for GitHub operations
# All forks and PRs will be created under the macroscope-gtm organization
# Required permissions: repo (full control of private repositories)
# Generate at: https://github.com/settings/tokens (using bot account)
GITHUB_BOT_TOKEN=your_github_bot_token_here

# GitHub OAuth (for user authentication)
# Create an OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# NextAuth Configuration
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Anthropic API Key (for PR Analysis feature)
# Required for the "PR Analysis" tab to analyze PRs using Claude
# Generate at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Apollo API Key (for email sequence integration)
# Optional: Required only if using the "Send to Apollo" feature
# Generate at: https://app.apollo.io/#/settings/integrations/api
APOLLO_API_KEY=your_apollo_api_key_here

# Attio API Key (for email sequence integration)
# Optional: Required only if using the "Send to Attio" feature
# Generate at: https://app.attio.com/settings/developers
# Required scopes: record_permission:read-write, object_configuration:read
ATTIO_API_KEY=your_attio_api_key_here

# Cloudflare R2 Storage (for code snippet images in emails)
# Optional: Required only if using code snippet image generation
# Create bucket at: https://dash.cloudflare.com → R2
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=macroscope-code-snippets
R2_PUBLIC_DOMAIN=https://your-bucket.your-account.r2.dev
